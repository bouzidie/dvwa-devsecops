name: DVWA DevSecOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Test pipeline
      run: |
        echo "Pipeline DevSecOps DVWA fonctionne"
